# Sleeping Pets Prototype v10

V10 basiert exakt auf der funktionierenden V7.

Einzige Änderung:
- Position des Mops-Canvas korrigiert.
- Keine Änderungen an HTML.
- Keine Änderungen an JavaScript.
- Keine Änderungen an Sprite-Sheets.
- Keine Änderungen am restlichen Layout.

Ziel: V7 behalten, nur den zu hoch sitzenden Mops nach unten setzen.
